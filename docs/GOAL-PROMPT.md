# /goal prompt — arbgrid completion (paste into a fresh session)

/goal Complete the arbgrid project (`~/Dev/polymarket-arb-scanner`) to its definition of done: a profitable, 24/7 automated prediction-market trading bot on Railway — 29 strategies / 5 risk layers across 8 platforms — with detection, execution, risk, MM, monitoring, and backtesting all production-grade. Done per CLAUDE.md = (1) net-positive P&L in trades.db over a 7-day live period, (2) <5% false-positive rate on detected opportunities, (3) ≥1 profitable round-trip executed without human intervention. Read CLAUDE.md and `docs/strategy-framework-v2.md` first; the tracker at `~/Financial Markets with AI/01-EXECUTION-TRACKER.md` is the authoritative queue and `.codebase-audit/report-20260717-consolidated.md` is the authoritative defect list.

CONTEXT SNAPSHOT (2026-07-20):
- Production: Railway bot UP on master (`0d12bbf`), but `DRY_RUN=true` — it has NEVER traded live. 1300+ consecutive scans with ZERO opportunities above threshold. The zero-opportunity question gates the entire business case.
- The Kalshi WS orderbook CRITICAL (C-1) is FIXED and merged (PR #98 → master `0d12bbf`).
- A 48h Kalshi MM dry-run soak died at ~23h (clean external stop). Restart-vs-accept + canary go/no-go are OPERATOR-ONLY decisions — never restart the runner or approve a canary yourself.
- Local checkout is frozen PR #62 branch (`fix/layer4-continuous-wiring`, 4 commits ahead incl. unmerged canary work) — work from a fresh worktree off master, don't touch this branch's history.
- Open PR backlog: 6 draft audit-report PRs (#79/#80/#81/#86/#88/#89) + non-draft #87, frozen #62, stale #25.
- `SUPABASE_SERVICE_ROLE_KEY` and `SLACK_WEBHOOK_URL` are empty placeholders in Infisical arbgrid/dev — lease + Slack escalation are down (operator provisioning pending; do not retry provisioning).

WORK PLAN — execute in order, one bounded PR per slice, tests green before merge, CodeRabbit review on every PR, auto-merge only on non-money-authority PRs (money-authority = order-placement paths, venue allowlists, capital caps, gate thresholds, treasury/broker config → BOTH reviewers clean AND operator 1-tap, auto-merge NEVER armed):

1. ANSWER THE ZERO-OPP QUESTION (highest leverage). Instrument the detection funnel: for each scan cycle log candidates found at mid-price stage, drops at CLOB-refine stage, and drops at threshold stage (MIN_NET_ROI, MIN_LIQUIDITY, fees). Then determine empirically whether zero opportunities = thresholds too tight, fee model too pessimistic, market reality (no arbs exist at our latency), or a detection bug. Deliver a written verdict with data. This decides whether the project pivots (e.g., toward the MM/rewards lane) or tunes.

2. FIX EXECUTION-PATH HIGHS from `.codebase-audit/report-20260717-consolidated.md` (~14, adversarially verify each before fixing — severities are finder-reported): cross-platform settlement/inversion bugs (`continuous.py:546`, `scans/cross.py:276`, `:466`); wrong LAY decimal-odds conversion (`executor.py:3095`); hedger ignoring max_loss/stale fill price (`hedger.py:250`); orphan-recovery stuffing order_id into token_id (`recovery.py:266`); MISSING DRY_RUN GUARDS on live-order paths (`sxbet_api.py:340`, `gemini_api.py:463/565` — incl. `withdraw_usdc` moving real funds ungated: money-authority, operator 1-tap); swallowed CRITICAL loss alert (`alerting.py:192`); missing CSRF on fund-transfer dashboard POSTs (`dashboard.py:384`); dead signal paths (`superforecaster_api.py:269`, `ws_feeds.py:1302`, `continuous.py:1881`). Every fix lands with a regression test that fails on the old code.

3. TEST-INTEGRITY REMEDIATION: the "3,017 passed" figure is inflated. Fix the tautological cluster — tests that reimplement production logic locally (`test_continuous.py` bankroll/resolution, `test_cli.py`/`test_scanner.py` private ArgumentParsers vs the real CLI parser, `test_credential_health.py` vacuous `if alert.called:`, `test_treasury.py` codifying double live withdrawal). Add first tests for zero-coverage modules: `calibration_tracker.py`, `twitter_api.py`, `reddit_api.py`, `scans/bracket.py`, `scans/settlement_timing.py`, `db.set_trade_order_id`, `recovery._convert_orphans_to_partial_fills`.

4. PR BACKLOG TRIAGE: rebase PR #62 onto master and land it (its canary + Layer-4 wiring work, reconciling anything master already fixed); close-or-convert the 7 audit-report PRs (#79–#89) — their findings are superseded by the 07-17 consolidated report, so convert still-valid unique findings to GitHub issues and close the PRs; close stale #25 after salvaging any still-relevant tests.

5. UNTRACKED WIP: commit or discard `scripts/kalshi_rewards_monitor.py`, `scripts/polymarket_rewards_monitor.py`, `docs/plans/07-hyperliquid-builder-routing-integration.md`; gitignore the `data/` dirs and generated `backtest_recommendations.json`.

6. GO-LIVE READINESS PACKAGE (prepare, do NOT flip): with 1–5 done, produce a single operator decision document: soak evidence requirements, canary plan (Kalshi-only, capped size), kill-switch verification, the DRY_RUN=false checklist, and the exact Railway env changes needed. Flipping DRY_RUN, funding, tranche advances, and venue allowlist changes are ALL operator-only.

HARD CONSTRAINTS (non-negotiable): never place live orders or move funds (DRY_RUN flips, canaries, withdrawals, rebalances = operator-only); never force-push or PR from `origin/fix/audit-77-verified` (completed work sits in `~/Dev/pm-arb-audit77 @ d2745bb` awaiting operator go-ahead); never restart the killed D0 soak runner; additive-only DB migrations; no test/coverage deletion; secrets only via Infisical CLI pipe (`infisical run --env dev -- <cmd>`), never in context; Polymarket is US close-only per geoblock — build/shadow-test only; scanner.py stays a pure re-export facade; follow the repo's two-stage scan pattern and `_build_legs`/`_revalidate` conventions when touching strategies. Record each completed slice with a dated entry in the execution tracker. When everything remaining is operator-gated, write an OPERATOR BLOCKERS summary and stop.
